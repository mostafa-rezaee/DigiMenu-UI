@page "{userId?}"
@using DigiMenu.Razor.Services.Roles
@model DigiMenu.Razor.Pages.Admin.Users.Roles.IndexModel
@inject IRoleService _roleService;
@{
    ViewData["Title"] = "مدیرت نقش ها";
    ViewData["selected"] = "users";
    var roles = await _roleService.GetRoles();
}

<h3>در دست اقدام ...</h3>
@* <h3>@ViewData["title"]</h3>
<a href="#" data-toggle="modal" data-target="#exampleModal" class="btn btn-success mb-2">افزودن نقش</a>

<div class="table-responsive card card-body p-0">
    <table class="table table-bordered mb-0">
        <thead>
            <tr>
                <th>عنوان نقش</th>
                <th>عملیات</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Images.OrderByDescending(d => d.Order))
            {
                <tr>
                    <td>
                        @item.RoleTitle
                    </td>
                    <td>
                        <delete-item url="@Url.Page("Index","DeleteItem",new {id=item.Id})">حذف</delete-item>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

<div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg" role="document">
        <form method="post" data-ajax="true" enctype="multipart/form-data" class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">افزودن نقش</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <div class="row">
                    <div class="col-md-6">
                        <select asp-for="CategoryId" class="form-control select2">
                            <option value="0">انتخاب دسته بندی</option>
                            @foreach (var item in roles)
                            {
                                <option value="@item.Id">@item.Title</option>
                            }
                        </select>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">بستن</button>
                <button type="submit" class="btn btn-success">ذخیره اطلاعات</button>
            </div>
        </form>
    </div>
</div> *@
